<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.chadamine.growbuddy.CultivationActivity$PlaceholderFragment" 
	>
	<RelativeLayout 
		android:id="@+id/llNavList"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:orientation="horizontal"
		style="@style/list_box_container"
    >
		<TextView 
			android:id="@+id/tvNavTitle"
			android:layout_height="30dp"
			android:layout_width="match_parent"
			style="CULTIVATION ACTIVITIES"
			android:text="@string/nav_title"
        />

		<ListView
			android:id="@android:id/list"
			style="@style/list_box"
			android:layout_width="match_parent"
			android:layout_height="221dp"
			android:layout_below="@+id/tvNavTitle"
			android:layout_marginTop="1dp" />
	</RelativeLayout>       
</RelativeLayout>
